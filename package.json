{
  "name": "@nodevisor/root",
  "description": "Server Management and Scalable Application Deployment",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "repository": {
    "url": "https://github.com/nodevisor/nodevisor"
  },
  "scripts": {
    "prettier": "prettier --write src/**/*.ts test/**/*.ts",
    "build": "lerna run build",
    "build:nodevisor": "lerna run build --scope=nodevisor --include-dependencies",
    "test": "lerna run test",
    "test:nodevisor": "lerna run test --scope=nodevisor --include-dependencies",
    "test:debian": "docker run --rm -v $(pwd):/workspace -w /workspace debian:buster /bin/bash -c 'apt-get update && apt-get install -y build-essential curl && curl -fsSL https://deb.nodesource.com/setup_18.x | bash - && apt-get install -y nodejs && npm install && npm test'",
    "test:ubuntu": "docker run --rm -v $(pwd):/workspace -w /workspace ubuntu:latest /bin/bash -c 'apt-get update && apt-get install -y build-essential curl && curl -fsSL https://deb.nodesource.com/setup_18.x | bash - && apt-get install -y nodejs && npm install && npm test'",
    "test:centos": "docker run --rm --dns 8.8.8.8 --dns 8.8.4.4 -v $(pwd):/workspace -w /workspace centos:7 /bin/bash -c 'yum update -y && yum groupinstall -y \"Development Tools\" && yum install -y curl && curl -fsSL https://rpm.nodesource.com/setup_18.x | bash - && yum install -y nodejs && npm install && npm test'",
    "test:fedora": "docker run --rm -v $(pwd):/workspace -w /workspace fedora:latest /bin/bash -c 'dnf update -y && dnf groupinstall -y \"Development Tools\" && dnf install -y curl && curl -fsSL https://rpm.nodesource.com/setup_18.x | bash - && dnf install -y nodejs && npm install && npm test'",
    "publish": "lerna publish --no-private --yes",
    "patch": "lerna patch --no-private",
    "version": "lerna version --no-private"
  },
  "dependencies": {},
  "devDependencies": {
    "lerna": "^8.1.8",
    "prettier": "^3.3.3"
  }
}
